# app/models/main_model.py
# Example model file (move your models here from app/models.py)

# Example SQLAlchemy model (uncomment if using SQLAlchemy)
# from app import db
# class User(db.Model):
#     id = db.Column(db.Integer, primary_key=True)
#     username = db.Column(db.String(64), unique=True, nullable=False)
